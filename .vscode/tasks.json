{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Sphinx autobuild",
            "command": "kill $(pidof python3); cd docs && sphinx-autobuild . build/html --host 0.0.0.0",
            "type": "shell",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}